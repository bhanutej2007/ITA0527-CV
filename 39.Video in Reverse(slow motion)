import cv2

def play_video_reverse_slow(video_path):

    cap = cv2.VideoCapture(video_path)

    frames = []

    while cap.isOpened():
        ret, frame = cap.read()

        if not ret:
            break

        frames.append(frame)

    cap.release()

    for frame in reversed(frames):

        # Resize frame slightly so the window is smaller
        frame = cv2.resize(frame, None, fx=0.7, fy=0.7)

        cv2.imshow('Reverse Slow Motion Video', frame)

        if cv2.waitKey(100) & 0xFF == ord('q'):  # slow motion
            break

    cv2.destroyAllWindows()


play_video_reverse_slow(r"C:\Users\bhanu\OneDrive\Pictures\video.mp4")
